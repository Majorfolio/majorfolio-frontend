import { styled } from 'styled-components';
import { StyledCenteredRow } from '../../../components/common/Row/index.styles';

const StyledRow = styled(StyledCenteredRow)`
  margin-top: 20px;
  margin-bottom: 8px;
`;

export default StyledRow;
