import { styled } from 'styled-components';

import Text from '../../../components/common/Text';

const StyledPreservedText = styled(Text)`
  white-space: pre-wrap;
`;

export default StyledPreservedText;
