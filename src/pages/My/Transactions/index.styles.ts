import styled from 'styled-components';
import Card from '../../../components/common/Card';
import Column from '../../../components/common/Column';

const StyledTransactionCardContainer = styled(Column)`
  box-shadow: 0px 0px 16px 0px rgba(35, 38, 41, 0.04);
  box-sizing: border-box;
`;

export default StyledTransactionCardContainer;
