import { styled } from 'styled-components';
import Column from '../Column';
import StyledColumn from '../Column/index.styles';

const StyledContainer = styled(StyledColumn)`
  height: 100%;
`;

export default StyledContainer;
